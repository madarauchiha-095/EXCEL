{
  "name": "delivery-routing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node tests/runAll.js",
    "backend": "node server.js",
    "osrm:start": "docker run --rm -t -i -p 5000:5000 -v \"%cd%/osrm-data:/data\" osrm/osrm-backend osrm-routed --algorithm mld /data/bangalore.osrm"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
